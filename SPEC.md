# 文件和应用管理器 - 规格说明书

## 1. 项目概述

- **项目名称**: File & App Hub
- **项目类型**: 单页Web应用 (SPA)
- **核心功能**: 本地文件和小工具应用的管理面板
- **目标用户**: 个人用户管理自己的文件和应用

## 2. UI/UX 规格

### 布局结构

- **顶部导航栏** (60px高度)
  - 左侧：Logo + 应用名称
  - 右侧：搜索框、添加按钮

- **侧边栏** (240px宽度)
  - 文件分类导航（全部文件、文档、图片、视频、小工具）
  - 应用分类导航（我的小工具）

- **主内容区** (自适应)
  - 文件/应用网格视图
  - 支持卡片式布局

- **底部状态栏** (40px)
  - 显示存储使用情况

### 响应式断点

- 桌面端: >= 1024px (侧边栏展开)
- 平板端: 768px - 1023px (侧边栏可折叠)
- 移动端: < 768px (侧边栏隐藏，底部导航)

### 视觉设计

**配色方案**
- 主色: #1a1a2e (深蓝黑)
- 次色: #16213e (深海蓝)
- 强调色: #e94560 (珊瑚红)
- 辅助色: #0f3460 (靛蓝)
- 文字主色: #eaeaea
- 文字次色: #a0a0a0
- 背景色: #0f0f1a
- 卡片背景: #1a1a2e
- 悬停高亮: #e94560

**字体**
- 主字体: "Segoe UI", "Microsoft YaHei", sans-serif
- 标题: 24px (h1), 20px (h2), 16px (h3)
- 正文: 14px
- 小字: 12px

**间距系统**
- 基础单位: 8px
- 卡片间距: 16px
- 区块间距: 24px

**视觉效果**
- 卡片阴影: 0 4px 20px rgba(0, 0, 0, 0.3)
- 圆角: 12px (卡片), 8px (按钮), 4px (输入框)
- 过渡动画: 0.3s ease

### 组件列表

1. **导航栏组件**
   - Logo区域
   - 搜索输入框 (带图标)
   - 添加按钮 (点击展开下拉菜单)

2. **侧边栏组件**
   - 分类列表项 (带图标和计数)
   - 选中状态高亮

3. **文件卡片组件**
   - 文件图标 (根据类型)
   - 文件名
   - 文件大小
   - 上传时间
   - 操作按钮 (查看/删除)

4. **应用卡片组件**
   - 应用图标
   - 应用名称
   - 应用描述
   - 启动按钮
   - 删除按钮

5. **模态框组件**
   - 上传文件模态框
   - 添加应用模态框
   - 确认删除模态框

6. **拖拽上传区域**
   - 虚线边框
   - 拖拽提示文字
   - 点击上传

## 3. 功能规格

### 核心功能

**文件管理**
- 支持上传文件 (文档、图片、视频、小工具代码)
- 文件分类显示 (按类型筛选)
- 文件搜索 (按名称)
- 文件预览 (图片/视频)
- 文件删除
- 文件大小显示
- 上传时间记录

**应用管理**
- 添加小工具应用 (名称、描述、图标URL、启动链接)
- 启动应用 (新窗口打开)
- 应用搜索
- 删除应用

**数据存储**
- 使用 IndexedDB 存储文件数据
- 使用 localStorage 存储应用配置

### 用户交互流程

1. **上传文件**
   - 点击添加按钮 → 选择"上传文件"
   - 拖拽或点击选择文件
   - 自动识别文件类型并分类

2. **添加应用**
   - 点击添加按钮 → 选择"添加小工具"
   - 填写应用名称、描述、图标链接、启动链接
   - 保存后显示在应用列表

3. **启动应用**
   - 点击应用卡片上的"启动"按钮
   - 在新窗口中打开应用

4. **删除操作**
   - 点击删除按钮
   - 弹出确认对话框
   - 确认后删除

## 4. 验收标准

- [x] 页面正常加载，无控制台错误
- [x] 侧边栏分类可点击切换
- [x] 可上传文件并显示在对应分类
- [x] 可添加小工具应用
- [x] 可启动小工具应用
- [x] 可删除文件和应用
- [x] 搜索功能正常工作
- [x] 数据持久化存储 (刷新后保留)
- [x] 响应式布局正常显示
- [x] 动画过渡流畅